Script will traverse through the conversations and return a data object containing the rating and token cost of each conversation between the user and the AI Agent.
To run script:
+ clone
+ npm i
+ node index.js
